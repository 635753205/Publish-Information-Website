extends layout

block content
    h1 Title: #{information.title}

    title
    information
    info_instance

    p #[strong Author:]
        a(href=information.author.url) #{information.author.name}
    p #[strong Contents:] #{information.contents}

    p #[strong Genres:] 
        a(href=information.genre.url) #{information.genre.name}

    